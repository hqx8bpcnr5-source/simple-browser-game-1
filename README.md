# Simple Browser Game

簡単なブラウザゲームのテンプレート。

- 開発方法: `index.html` をブラウザで開くか、ローカルサーバーを起動してアクセスします。

簡単な実行例（プロジェクトルートで）:

```bash
# Python 3 の簡易サーバー
python3 -m http.server 8000
# ブラウザで開く: http://localhost:8000/
```

ゲーム内容: キャンバス上に移動する円をクリックして得点を稼ぎます。時間切れになるとスコアが表示されます。
